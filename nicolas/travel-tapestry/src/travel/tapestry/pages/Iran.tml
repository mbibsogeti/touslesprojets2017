<html xmlns:t="http://tapestry.apache.org/schema/tapestry_5_4.xsd"
	t:type="Layout" title="Iran " xmlns:p="tapestry:parameter">
	
<!-- ATTENTION A D2CLARER  LES PR2FIXE P ouT ..;  P: est toujour a l intérieu d'une balise T: car c des parametrtre-->
	<h1>Voyage en iran</h1>


	<div class="row">

		<!-- le contxt: désigenra tjr la racine du projet donc ou sont bien les 
			pages -->
	</div>
	<p>n'attendez pas :${discount.name} pour seulement ${discount.price}
		mais ${discount.description}</p>
		<!-- permet d'avoir un affichage jolie , il récupère les noms des propriétés disponible dans la classe en cherchan les get et public ..-->
			<t:beandisplay object="discount" reorder="name,price,description">
			
			<!-- description affiche les 10 premier : le caractère 0 au 10 em exclu -->
			<p:description>${discount.description.substring(0,4)}</p:description></t:beandisplay>

	<p>${travels.size()} voyages : </p>
	
	<!-- syntaxe de loop : -->
	<!-- <t:loop></t:loop> //première syntaxe de boucle -->
	<!-- <t:loop source="travels" value="travel">${travel.name}</t:loop> -->
	
	<!-- syntaxe de if 
	<t:if test=""> </t:if>  ou
	<div (ou autre balise ) t:type="if"</div>
	
	 -->
	<table class="table table-striped" >
	<tr><th>Nom</th>
	<th>prix</th>
	<th>desc</th>
	<th>prix riad</th>
	<th>i</th>
	
	</tr>
	<tr t:type="loop" source="travels" value="travel" index="i" empty="aucun voyage">
		${prop:i}<td>${travel.name}</td> <!-- le prop ou literal n'affiche que si liste vide ) prop désigne une propriété-->
		<td>${travel.price}</td>
		<td>${travel.description}</td>
		<td>${rialPrice}<t:if test="cheap">!</t:if></td>
		<!-- le if  dessous utilise le test cheap qui renvoi a la fonction de isCHeap dans le iran.java -->
		
		<td>${iplusone}</td>
		
	</tr>
	</table>
</html>