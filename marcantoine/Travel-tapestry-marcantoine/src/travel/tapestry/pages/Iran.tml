<html xmlns:t="http://tapestry.apache.org/schema/tapestry_5_4.xsd" xmlns:p="tapestry:parameter"
	t:type="layout" title="Our trips in Iran">
	<h1>Our trips in Iran</h1>
	<section class="row" style="padding:15px">
		<section class="col-xs-6">
			<img src="${context:img/iran.jpg}" class="img-circle" />
		</section>
		<section class="col-xs-6">
			<h2>Some details about this destination...</h2>
			<p>Our first destination is ${discount.name}. Here is a small
				description of the place: ${discount.desc}. We have a very
				attractive
				offer currently. Just ${discount.price} the trip!</p>
		</section>
	</section>
	<section class="row">
		<section class="col-xs-6">
			<h2>${travels.size()} trips are currently available: </h2>
			<t:beandisplay object="discount" reorder="name,price,desc"><p:name>${discount.name.toUpperCase()}</p:name><p:desc>${discount.desc.substring(0,10)}</p:desc></t:beandisplay>
			<table class="table table-striped">
				<tr>
					<th>Index</th>
					<th>Name</th>
					<th>Description</th>
					<th>Price (€)</th>
					<th>Price (Rials)</th>
				</tr>
				<t:loop source="travels" value="travel" index="i">
					<tr>
						<td>${iPlusOne}</td>
						<td>${travel.name}</td>
						<td>${travel.desc}</td>
						<td>${travel.price}<div t:type="if" test="${promo}">!</div></td>
						<td>${RialPrice}<t:if test="${promo}"><span class="glyphicon glyphicon-euro"></span></t:if></td>
					</tr>
				</t:loop>
			</table>
		</section>
		<section class="col-xs-6">
		</section>
	</section>
</html>