<!DOCTYPE web-app
      PUBLIC "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
      "http://java.sun.com/dtd/web-app_2_3.dtd">

<web-app>
	<display-name>My Tapestry Application</display-name>
	<context-param>
		<param-name>tapestry.app-package</param-name>
		<param-value>fr.ib.travel.tapestry</param-value>
	</context-param>

	<!-- Un drapeau (vrai ou faux) indiquant si l'application est en cours de 
		production ou en cours de développement. La valeur par défaut est true, ce 
		qui signifie que les exceptions d'exécution ne sont pas signalées avec tous 
		les détails (seul le message d'exception de la racine est affiché et non 
		la pile entière d'exceptions, les propriétés et les autres informations affichées 
		en mode de développement). -->
	<context-param>
		<param-name>tapestry.production-mode</param-name>
		<param-value>false</param-value>
	</context-param>

	<!-- Un drapeau (vrai ou faux). Lorsque vrai, l'espace (par défaut) 
		des modèles de composants est compressé par défaut (cela peut être réglé 
		à l'aide de l'attribut standard xml: space d'un élément du modèle). Lorsque 
		ce drapeau est faux, l'espaces est conservé par défaut (mais peuvent 
		encore être remplacés). -->
	<context-param>
		<param-name>tapestry.compress-whitespace</param-name>
		<param-value>false</param-value>
	</context-param>
	
	<context-param>
		<param-name>tapestry.application-version</param-name>
		<param-value>1</param-value>
	</context-param>

	<filter>
		<filter-name>app</filter-name>
		<filter-class>org.apache.tapestry5.TapestryFilter</filter-class>
	</filter>
	<filter-mapping>
		<filter-name>app</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
</web-app>