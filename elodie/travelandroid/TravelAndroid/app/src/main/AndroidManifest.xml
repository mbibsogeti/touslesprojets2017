<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="fr.ib.travelandroid">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".MapActivity"><!--Fait référence à la classe MapActivity du package inscrit au-dessus. on a ici une activité de démarrage!!-->
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>
        <activity android:name=".SubmapsActivity"> <!--Activité simple - activité caché-->
        </activity>
        <activity android:name=".PlaneActivity"> <!--Activité simple - activité caché-->
        </activity>
    </application>

</manifest>
